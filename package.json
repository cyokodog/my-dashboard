{
  "name": "my-dashboard-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy",
    "deploy-no-git": "npm run build && mv .git .git.backup && npx wrangler pages deploy .svelte-kit/cloudflare --project-name=my-dashboard-app && mv .git.backup .git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "@sveltejs/adapter-cloudflare": "^7.2.4",
    "@sveltejs/kit": "^2.46.5",
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@types/node": "^24.7.2",
    "svelte": "^5.40.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.10",
    "wrangler": "^4.43.0"
  },
  "dependencies": {
    "marked": "^16.4.0",
    "tsdav": "^2.1.5"
  }
}
